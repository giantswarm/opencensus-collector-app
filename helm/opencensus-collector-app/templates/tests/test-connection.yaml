apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opencensus-collector-app.fullname" . }}-test-connection"
  labels:
{{ include "opencensus-collector-app.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "opencensus-collector-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
